<table>
  <tr>
    <th>Done</th>
    <th>Task</th>
    <th>Description</th>
    <th>Created</th>
    
  </tr>
<% if @task == @current_task %>
<tr id = "current_task">
<% else %>
<tr>
<% end %>

<% @tasks.each do |task| %>
  <tr>
  <td>
  <%= form_for(task) do |f| %>
    <%= f.hidden_field :finished, :value => !task.finished %>
    <div class="actions">
      <% if task.finished %>
        <%= image_submit_tag 'finished.png' %>
      <% else %>
        <%= image_submit_tag 'unfinished.png' %>
      <% end %>
    </div>
  <% end %>
  </td>
    <td><%= task.name %></td>
    <td><%= task.description %></td>
    <td><%= task.created_at.to_s(:simple_date) %></td>
   
    </p>
    
    <td><%= link_to 'Edit', edit_task_path(task) %>
    <%= link_to 'Delete', task, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>
<hr />
<%= link_to 'Add Another Task', new_task_path %>